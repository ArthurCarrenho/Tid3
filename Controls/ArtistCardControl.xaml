<?xml version="1.0" encoding="utf-8"?>

<UserControl
    x:Class="TidalUi3.Controls.ArtistCardControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:TidalUi3.Controls"
    x:Name="Root"
    mc:Ignorable="d">

    <StackPanel Width="140" Margin="0,0,8,0" Spacing="8">
        <controls:ImagePlaceholderControl
            Width="140" Height="140"
            ImageSource="{Binding ArtistImageUrl, ElementName=Root}"
            FallbackGlyph="&#xE77B;" IconSize="40"
            ImageCornerRadius="70" />
        <TextBlock Text="{Binding ArtistName, ElementName=Root}" HorizontalAlignment="Center"
                   Style="{StaticResource BodyStrongTextBlockStyle}"
                   TextTrimming="CharacterEllipsis" MaxLines="1" />
        <TextBlock Text="Artist" HorizontalAlignment="Center"
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
    </StackPanel>
</UserControl>
