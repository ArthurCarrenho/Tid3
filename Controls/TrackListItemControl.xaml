<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="TidalUi3.Controls.TrackListItemControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:TidalUi3.Controls"
    mc:Ignorable="d"
>

    <Grid Padding="4,8" ColumnSpacing="14" Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="RowNumberColumnDef" Width="32" />
            <ColumnDefinition Width="48" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition x:Name="AlbumColumnDef" Width="0.5*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition x:Name="HeartColumnDef" Width="Auto" />
            <ColumnDefinition Width="56" />
        </Grid.ColumnDefinitions>

        <!-- Column 0: Row Number -->
        <TextBlock
            x:Name="RowNumberText"
            Text="{Binding RowNumber}"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
            Style="{StaticResource CaptionTextBlockStyle}"
        />

        <!-- Column 1: Cover -->
        <controls:ImagePlaceholderControl
            Grid.Column="1"
            Width="48"
            Height="48"
            ImageSource="{Binding CoverUrl}"
            FallbackGlyph="&#xE8D6;"
            IconSize="18"
            ImageCornerRadius="6"
        />

        <!-- Column 2: Title and Artist -->
        <StackPanel Grid.Column="2" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" Spacing="6">
                <TextBlock
                    Text="{Binding Title}"
                    Style="{StaticResource BodyStrongTextBlockStyle}"
                    TextTrimming="CharacterEllipsis"
                    MaxLines="1"
                />
                <controls:ExplicitBadgeControl
                    IsExplicit="{Binding IsExplicit}"
                />
            </StackPanel>
            <TextBlock
                Text="{Binding Artist}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextTrimming="CharacterEllipsis"
                MaxLines="1"
            />
        </StackPanel>

        <!-- Column 3: Album -->
        <TextBlock
            x:Name="AlbumText"
            Grid.Column="3"
            Text="{Binding Album}"
            VerticalAlignment="Center"
            Style="{StaticResource CaptionTextBlockStyle}"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            TextTrimming="CharacterEllipsis"
            MaxLines="1"
        />

        <!-- Column 4: Quality -->
        <controls:QualityBadgeControl
            Grid.Column="4"
            Quality="{Binding QualityBadge}"
        />

        <!-- Column 5: Heart -->
        <FontIcon
            x:Name="HeartIcon"
            Grid.Column="5"
            Glyph="&#xEB52;"
            FontSize="14"
            Foreground="#E91E63"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Visibility="Collapsed"
        />

        <!-- Column 6: Duration -->
        <TextBlock
            Grid.Column="6"
            Text="{Binding Duration}"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Style="{StaticResource CaptionTextBlockStyle}"
            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
        />
    </Grid>
</UserControl>
