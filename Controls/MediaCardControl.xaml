<?xml version="1.0" encoding="utf-8"?>

<UserControl
    x:Class="TidalUi3.Controls.MediaCardControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:TidalUi3.Controls"
    x:Name="Root"
    mc:Ignorable="d">

    <Grid Width="{Binding CardWidth, ElementName=Root}" Margin="0,0,8,0" CornerRadius="8"
          Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
        <StackPanel>
            <controls:ImagePlaceholderControl
                Height="{Binding CardWidth, ElementName=Root}"
                ImageSource="{Binding ImageUrl, ElementName=Root}"
                FallbackGlyph="{Binding FallbackGlyph, ElementName=Root}"
                IconSize="48"
                ImageCornerRadius="8,8,0,0" />
            <StackPanel Padding="12,10" Spacing="2">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <TextBlock Text="{Binding TitleText, ElementName=Root}"
                               Style="{StaticResource BodyStrongTextBlockStyle}" TextTrimming="CharacterEllipsis"
                               MaxLines="1" />
                    <controls:ExplicitBadgeControl IsExplicit="{Binding IsExplicit, ElementName=Root}" />
                </StackPanel>
                <TextBlock Text="{Binding SubtitleText, ElementName=Root}"
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis"
                           MaxLines="1" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
